receivers:
  nop:

processors:

exporters:
  loadbalancerhttp:
    protocol:
      otlp:
        endpoint: should-be-replaced

    resolver:
      static:
        hostnames:
        - endpoint-1
service:
  pipelines:
    traces:
      receivers:
      - nop
      processors: []
      exporters:
      - loadbalancerhttp
    logs:
      receivers:
        - nop
      processors: []
      exporters:
        - loadbalancerhttp
